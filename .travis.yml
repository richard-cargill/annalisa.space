language: node_js
node_js:
    - "stable"
cache:
  directories:
    - node_modules
deploy:
  provider: pages
  skip-cleanup: true
  local_dir: public
  keep-history: true
  on:
    branch: master
before_script:
  - "npm i"
after_success:
  - "npm run deploy"
